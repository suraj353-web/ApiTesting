pipeline {
    agent any

    stages {

        stage('Install Dependencies') {
            steps {
                bat 'npm install'
            }
        }

        stage('Run Specific API Test') {
            steps {
                bat 'npx playwright test tests/ApiTesting.spec.ts'
            }
        }

    }
}
